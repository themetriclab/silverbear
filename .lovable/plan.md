## Goal
Use the newly uploaded polar-bear-in-yellow-grass photo as the header image for the **"Best Polar Bear Tour"** blog post.

## Steps
1. Copy the uploaded `Bear-in-yellow-grass.jpg` over `src/assets/slider/bear-yellow-grass.jpg` (overwrite the current one).
2. No code change needed — `src/data/blog.ts` already imports `bear-yellow-grass.jpg` for this post, so the new image flows to both the listing card and the post header automatically.